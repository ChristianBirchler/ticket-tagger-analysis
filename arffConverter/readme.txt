1) arff converter on ticket tagger dataset. receive dataset x

2) preprocess dataset x with StringToWordVector filter

( 	3) if it still doesn't work, maybe use NumericToNominal or NumericToBinary filter 	)


4) Use Cross Validation